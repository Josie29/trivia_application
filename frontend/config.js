/**
 * API origin for fetch() and EventSource (no trailing slash).
 *
 * - Local (page served by FastAPI): leave "" so URLs are same-origin.
 * - GitHub Pages: set to your deployed backend, e.g. "https://your-service.onrender.com"
 */
window.__TRIVIA_API_BASE__ = "";
